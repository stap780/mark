<div class="space-x-1">
  <% mailganer = current_account.mailganer %>
  <% if mailganer.present? %>
    <%= link_to t('mailganers.test_email_label'),
          test_email_form_account_mailganer_path(current_account, mailganer),
          data: { turbo_frame: 'offcanvas' },
          class: "inline-flex items-center px-3 py-1 bg-green-600 text-white rounded-md hover:bg-green-700 text-sm" %>
  <% end %>
  <%= link_to_new new_account_mailganer_path(current_account), data: { turbo_frame: :offcanvas, turbo_prefetch: false } %>
</div>


