<div id="<%= dom_id(swatch_group) %>" class="grid grid-cols-4 gap-4 p-4 items-center">
  <div class="truncate"><%= swatch_group.name %></div>
  <div class="truncate"><%= swatch_group.option_name %></div>
  <div class="truncate"><%= swatch_group.status %></div>
  <div class="space-x-2">
    <%= link_to "Edit", edit_account_swatch_group_path(swatch_group.account, swatch_group), class: "text-violet-700 hover:underline" %>
    <%= link_to "Delete", account_swatch_group_path(swatch_group.account, swatch_group), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }, class: "text-red-600 hover:underline" %>
  </div>
</div>

