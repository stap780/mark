<%# Turbo frame + flowchart — для замены при create/update/destroy/update_position %>
<%= turbo_frame_tag dom_id(current_account, dom_id(automation_rule, :flowchart)) do %>
  <%= render "automation_rules/flowchart_canvas", automation_rule: automation_rule, current_account: current_account, layout: layout %>
<% end %>
